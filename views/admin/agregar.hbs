<div class="contadminagregnoticias">
    <div class="row">
        <div class="formagregar">
           <div class="col-6 offset-3 agrnoticia">
                <h4 class="tituagregar">Agregar una nueva noticia</h4>
                <form action="/admin/noticias/agregar" method="post">
                <div class="mb-3 row">
                    <input type="text" class="form-control" placeholder="Título" name="titulo">
                </div>
                <div class="mb-3 row">
                    <textarea name="cuerpo" type="text" class="form-control" placeholder="Cuerpo"></textarea>
                </div>
                <button type="submit" class="btn btn-primary botonform">Guardar</button>
            </form>  
            {{#if error}}
                <h5 class="erroragregar">{{message}}</h5> 
            {{/if}}
            </div> 
        </div>
        
    </div>
</div>